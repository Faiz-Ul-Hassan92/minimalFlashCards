Its a flashcards mini web app for question/answer practice. All contained within this folder, just run 

```npm install```

It will install the dependencies, I haven't used a huge database daemon, infact sqllite will make a file called 

**flashcards.db**

and that will hold the data for persistance.


After dependencies(node modules) are installed just run

*node server.js*